services:
  metadata-lab:
    build: .
    environment:
      HOST: 0.0.0.0
      PORT: 3000
      METADATA_LAB_TMP: /tmp/metadata-lab
    ports:
      - "127.0.0.1:3000:3000"
    read_only: true
    tmpfs:
      - /tmp:size=512m,mode=1777
