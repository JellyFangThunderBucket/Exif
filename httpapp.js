import http from 'node:http';import fs from'node:fs/promises';import fss from'node:fs';import path from'node:path';import {config}from'./config.js';import{buildExifArgs,ensureTempRoot,isAllowedMime,randomId,resolveTemp,runExiftool,safeBasename,presets}from'./exiftool.js';
const rates=new Map();
function json(res,code,obj){res.writeHead(code,{'content-type':'application/json'});res.end(JSON.stringify(obj))}function publicError(e){return /ENOENT/.test(e.message)?'ExifTool is not installed or not available.':e.message.replace(config.tempRoot,'[temporary directory]')}async function body(req){let a=[];for await(const c of req){a.push(c);if(Buffer.concat(a).length>config.maxUploadBytes+1e6)throw Error('Upload too large.')}return Buffer.concat(a)}async function parseMultipart(req){const ct=req.headers['content-type']||'',m=ct.match(/boundary=(.+)$/);if(!m)throw Error('Multipart form data is required.');const b=Buffer.from('--'+m[1]);const data=await body(req);const start=data.indexOf(b)+b.length+2,end=data.indexOf(Buffer.concat([Buffer.from('\r\n'),b]),start);const part=data.subarray(start,end);const headEnd=part.indexOf('\r\n\r\n');const head=part.subarray(0,headEnd).toString();const content=part.subarray(headEnd+4);const filename=(head.match(/filename="([^"]*)"/)||[])[1]||'upload';const type=(head.match(/Content-Type: ([^\r\n]+)/i)||[])[1]||'application/octet-stream';return{filename,type,content}}
function rate(req,res){const ip=req.socket.remoteAddress,now=Date.now(),r=rates.get(ip)||{n:0,t:now};if(now-r.t>config.rateLimitWindowMs){r.n=0;r.t=now}r.n++;rates.set(ip,r);if(r.n>config.rateLimitMax){json(res,429,{error:'Too many requests.'});return true}}
async function handler(req,res){try{if(rate(req,res))return;await ensureTempRoot();const url=new URL(req.url,'http://x');if(req.method==='GET'&&url.pathname==='/api/config')return json(res,200,{maxUploadBytes:config.maxUploadBytes,presets});if(req.method==='GET'&&url.pathname==='/api/about'){let exiftoolVersion='Unavailable';try{const v=await runExiftool(['-ver'],5000);if(v.code===0)exiftoolVersion=v.stdout.trim()||'Unavailable'}catch{}return json(res,200,{name:'Metadata Lab',version:'1.0.0',exiftoolVersion})}if(req.method==='POST'&&url.pathname==='/api/upload'){const f=await parseMultipart(req);if(!isAllowedMime(f.type))return json(res,400,{error:'Unsupported file type.'});if(f.content.length>config.maxUploadBytes)return json(res,413,{error:'Upload exceeds configured size limit.'});const fileId=randomId(),storedName=`${randomId()}-${safeBasename(f.filename)}`,dir=resolveTemp(fileId);await fs.mkdir(dir,{recursive:true,mode:0o700});await fs.writeFile(resolveTemp(fileId,storedName),f.content,{mode:0o600});return json(res,200,{fileId,filename:f.filename,storedName,type:f.type,size:f.content.length})}if(req.method==='POST'&&(url.pathname==='/api/command'||url.pathname==='/api/run')){const p=JSON.parse((await body(req)).toString()||'{}');const fileId=path.basename(p.fileId||''),storedName=path.basename(p.storedName||'');const inputPath=resolveTemp(fileId,storedName);const st=await fs.stat(inputPath).catch(()=>null);if(!st)return json(res,400,{error:'Uploaded file was not found or expired.'});const outputName=`out-${randomId()}-${storedName}`,outputPath=resolveTemp(fileId,outputName);const built=buildExifArgs({...p,inputPath,outputPath});if(url.pathname==='/api/command')return json(res,200,built);const result=await runExiftool(built.args);const exists=await fs.stat(outputPath).then(s=>s.isFile()).catch(()=>false);return json(res,200,{...built,...result,download:exists?`/api/download/${fileId}/${outputName}`:null,organized:organize(result.stdout)})}if(req.method==='POST'&&url.pathname==='/api/panic'){await fs.rm(config.tempRoot,{recursive:true,force:true});await ensureTempRoot();return json(res,200,{ok:true})}if(req.method==='GET'&&url.pathname.startsWith('/api/download/')){const [, , , fid, ...nm]=url.pathname.split('/');const p=resolveTemp(path.basename(fid),path.basename(nm.join('/')));res.writeHead(200,{'content-disposition':'attachment; filename="'+path.basename(p)+'"'});return fss.createReadStream(p).pipe(res)}let file=url.pathname==='/'?'/index.html':url.pathname;let p=path.resolve('public',file.slice(1));if(!p.startsWith(path.resolve('public')))return json(res,404,{error:'Not found.'});let data=await fs.readFile(p).catch(()=>null);if(!data)return json(res,404,{error:'Not found.'});res.writeHead(200,{'content-type':mime(p)});res.end(data)}catch(e){json(res,400,{error:publicError(e)})}}
function organize(stdout){const groups={};for(const line of stdout.split('\n')){const m=line.match(/^\[([^\]]+)\]\s*([^:]+):\s*(.*)$/)||line.match(/^([^:]+):\s*(.*)$/);if(m){const g=m.length===4?m[1]:'General',k=m.length===4?m[2]:m[1],v=m.length===4?m[3]:m[2];(groups[g]||=[]).push({key:k.trim(),value:v.trim()})}}return groups}function mime(p){return p.endsWith('.css')?'text/css':p.endsWith('.js')?'text/javascript':p.endsWith('.webmanifest')?'application/manifest+json':'text/html'}
export function createServer(){return http.createServer(handler)}
